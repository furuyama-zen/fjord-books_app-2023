<!DOCTYPE html>
<html>
  <head>
    <title>BooksApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <p class="navbar-text float-right">
  <% if user_signed_in? %>
    "ログイン中" <strong><%= current_user.email %></strong>.
    <%= link_to "プロフィールを編集する", edit_user_registration_path, class: "navbar-link" %> |
    <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "navbar-link"  %>
  <% else %>
    <%= link_to "アカウント登録", new_user_registration_path, class: "navbar-link"  %> |
    <%= link_to "ログイン", new_user_session_path, class: "navbar-link"  %>
  <% end %>
  </p>
  <body class="<%= params[:action] %>">

  <%= yield %>
  </body>
</html>
