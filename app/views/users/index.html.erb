<h1>ユーザーの一覧</h1>
<% @users.each do |user| %>
  <li>
  <%= user.email %>
  <%= link_to ('詳細'), user_path(user.id), class: "navbar-link" %>
  <% end %>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: i18n_pluralize(Book.model_name.human.downcase)), books_path %>
</nav>
